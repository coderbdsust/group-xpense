name: group_xpense
description: "A group expense manager with split functionality"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.4
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.1.2
  intl: ^0.20.0
  uuid: ^4.5.1
  shared_preferences: ^2.3.3
  sqflite: ^2.3.3+2
  path: ^1.9.0
  local_auth: ^2.2.0
  flutter_markdown: ^0.6.18

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
flutter:
  uses-material-design: true

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/icon.png"